# setup.sh scripts are executed by the eval runner via `bash`; CRLF line
# endings break them (bash treats a trailing \r as part of the command). Force
# LF regardless of the cloner's core.autocrlf setting.
*.sh text eol=lf
